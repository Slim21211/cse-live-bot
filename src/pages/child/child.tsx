import styles from './child.module.scss';

const Child = () => {
  return (
    <h1 className={styles.child}>
      Форма для участия в детском конкурсе скоро появится
    </h1>
  );
};

export default Child;
